dist: trusty
sudo: false

jobs:
  include:
    - stage: Update test results
      if: type != cron
      install: pip3 install junitparser
      script: python3 .bin/process.py
    - stage: Clean up
      if: type = cron
      script: bash .bin/clean.sh

notifications:
  email: false

